kind: Service
apiVersion: extensions/v1beta1metadata:
  name: pants
spec:
  selector:
    app: pants
  ports:
  - protocol: TCP
    port: 80
    targetPort: 80
---
kind: Deployment
apiVersion: extensions/v1beta1metadata:
  name: pants-deployment
  labels:
    app: pants
spec:
  replicas: 3
  selector:
    matchLabels:
      app: pants
  template:
    metadata:
      labels:
        app: pants
    spec:
      containers:
      - name: nginx
        image: nginx:1.7.9
        ports:
        - containerPort: 80
