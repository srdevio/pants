env.SERVICE_NAME = 'pants'

pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                echo "$SERVICE_NAME:$BRANCH_NAME"
                sh 'printenv'
            }
        }
        stage('Test') {
            steps {
                echo 'Testing..'
            }
        }
        stage('Deploy') {
            steps {
                echo 'Deploying....'
            }
        }
    }
}
